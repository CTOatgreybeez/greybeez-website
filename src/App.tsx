import React from "react"; nimport { useState } from "react"; nimport LocationLanguageSuggester from "./components/LocationLanguageSuggester"; nimport Header from "./components/Header"; nimport HeroCarousel from "./components/HeroCarousel"; nimport ImpactHighlights from "./components/ImpactHighlights"; nimport PartnerLogosCarousel from "./components/PartnerLogosCarousel"; nimport ProgramSpotlight from "./components/ProgramSpotlight"; nimport ServicesSnapshot from "./components/ServicesSnapshot"; nimport Testimonials from "./components/Testimonials"; nimport FinalCTA from "./components/FinalCTA"; nimport Footer from "./components/Footer"; nimport JobParPage from "./components/JobParPage"; nimport JobParAnnouncementPage from "./components/JobParAnnouncementPage"; nimport AboutUsHeroSection from "./components/AboutUsHeroSection"; nimport ProgramsPage from "./components/ProgramsPage"; nimport SuccessStoriesPage from "./components/SuccessStoriesPage";nimport ContactPage from "./components/ContactPage";nnfunction App() { n  const [currentPage, setCurrentPage] = useState("home"); n  n  const renderPage = () => { n    switch (currentPage) { n      case "jobpar": n        return <JobParAnnouncementPage />; n      case "about": n        return <AboutUsHeroSection />; n      case "programs": n        return <ProgramsPage />; n      case "stories": n        return <SuccessStoriesPage />; n      case "contact": n        return <ContactPage />; n      case "home": n      default: n        return ( n          <> n            <HeroCarousel /> n            <ImpactHighlights /> n            <PartnerLogosCarousel /> n            <ProgramSpotlight /> n            <ServicesSnapshot /> n            <Testimonials /> n            <FinalCTA /> n          </> n        ); n    } n  };nn  return ( n    <div className="font-inter"> n      <LocationLanguageSuggester /> n      <Header currentPage={currentPage} onPageChange={setCurrentPage} /> n      <main className="pt-16"> n        {renderPage()} n      </main> n      <Footer /> n    </div> n  ); n} nnexport default App;