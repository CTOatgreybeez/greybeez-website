import React, { useState } from 'react';nimport { useTranslation } from 'react-i18next';nimport { X, User, Phone, Send } from 'lucide-react';nninterface WorkerFormPopupProps {n  isOpen: boolean;n  onClose: () => void;n}nnexport default function WorkerFormPopup({ isOpen, onClose }: WorkerFormPopupProps) {n  const { t } = useTranslation();n  const [formData, setFormData] = useState({n    name: '',n    phone: ''n  });nn  const handleInputChange = (e: React.ChangeEvent<HTMLInputElement>) => {n    const { name, value } = e.target;n    setFormData(prev => ({n      ...prev,n      [name]: valuen    }));n  };nn  const handleSubmit = (e: React.FormEvent) => {n    e.preventDefault();n    // Handle form submission heren    console.log('Worker form submitted:', formData);n    // Reset formn    setFormData({ name: '', phone: '' });n    // Close popupn    onClose();n  };nn  if (!isOpen) return null;nn  return (n    <div className="fixed inset-0 z-50 flex items-center justify-center">n      {/* Backdrop */}n      <div n        className="absolute inset-0 bg-black/50 backdrop-blur-sm"n        onClick={onClose}n      ></div>n      n      {/* Modal */}n      <div className="relative bg-white rounded-2xl shadow-2xl max-w-md w-full mx-4 transform transition-all duration-300 scale-100">n        {/* Header */}n        <div className="flex items-center justify-between p-6 border-b border-gray-100">n          <div>n            <h2 className="text-2xl font-bold text-gray-900">n              {t('workerForm.title', 'Join Our Workforce')}n            </h2>n            <p className="text-gray-600 text-sm mt-1">n              {t('workerForm.subtitle', 'Start your journey with GreyBeez')}n            </p>n          </div>n          <buttonn            onClick={onClose}n            className="p-2 rounded-lg text-gray-400 hover:text-gray-600 hover:bg-gray-100 transition-colors duration-200"n          >n            <X className="w-5 h-5" />n          </button>n        </div>nn        {/* Form */}n        <form onSubmit={handleSubmit} className="p-6">n          <div className="space-y-4">n            {/* Name Field */}n            <div>n              <label htmlFor="name" className="block text-sm font-medium text-gray-700 mb-2">n                {t('workerForm.name', 'Full Name')} *n              </label>n              <div className="relative">n                <User className="absolute left-3 top-1/2 transform -translate-y-1/2 w-5 h-5 text-gray-400" />n                <inputn                  type="text"n                  id="name"n                  name="name"n                  value={formData.name}n                  onChange={handleInputChange}n                  requiredn                  className="w-full pl-10 pr-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-primary-500 transition-colors duration-300"n                  placeholder={t('workerForm.namePlaceholder', 'Enter your full name')}n                />n              </div>n            </div>nn            {/* Phone Field */}n            <div>n              <label htmlFor="phone" className="block text-sm font-medium text-gray-700 mb-2">n                {t('workerForm.phone', 'Phone Number')} *n              </label>n              <div className="relative">n                <Phone className="absolute left-3 top-1/2 transform -translate-y-1/2 w-5 h-5 text-gray-400" />n                <inputn                  type="tel"n                  id="phone"n                  name="phone"n                  value={formData.phone}n                  onChange={handleInputChange}n                  requiredn                  className="w-full pl-10 pr-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-primary-500 transition-colors duration-300"n                  placeholder={t('workerForm.phonePlaceholder', 'Enter your phone number')}n                />n              </div>n            </div>n          </div>nn          {/* Submit Button */}n          <buttonn            type="submit"n            className="w-full bg-gradient-to-r from-primary-600 to-secondary-600 text-white py-4 px-6 rounded-lg font-semibold hover:from-primary-700 hover:to-secondary-700 transform hover:scale-105 transition-all duration-300 flex items-center justify-center mt-6"n          >n            <Send className="w-5 h-5 mr-2" />n            {t('workerForm.submit', 'Submit Application')}n          </button>nn          {/* Privacy Note */}n          <p className="text-xs text-gray-500 text-center mt-4">n            {t('workerForm.privacy', 'By submitting this form, you agree to our privacy policy and consent to being contacted regarding opportunities.')}n          </p>n        </form>n      </div>n    </div>n  );n}