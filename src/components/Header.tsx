import React, { useState } from 'react';nimport { useTranslation } from 'react-i18next';nimport { Menu, X, Smartphone } from 'lucide-react';nimport LanguageSwitcher from './LanguageSwitcher';nninterface HeaderProps {n  currentPage: string;n  onPageChange: (page: string) => void;n}nnexport default function Header({ currentPage, onPageChange }: HeaderProps) {n  const { t } = useTranslation();n  const [isMenuOpen, setIsMenuOpen] = useState(false);nn  const navigation = [n    { name: t('header.home'), key: 'home' },n    { name: t('header.aboutUs'), key: 'about' },n    { name: t('header.programs'), key: 'programs' },n    { name: t('header.successStories'), key: 'stories' },n    { name: t('header.contact'), key: 'contact' }n  ];nn  return (n    <header className="fixed top-0 left-0 right-0 z-50 bg-transparent backdrop-blur-md border-b border-white/20">n      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">n        <div className="flex items-center h-20 md:h-24 gap-x-6">n          {/* Logo */}n          <div className="flex items-center py-2">n            <buttonn              onClick={() => onPageChange('home')}n              className="flex items-center hover:opacity-80 transition-opacity duration-300 focus:outline-none focus:ring-2 focus:ring-primary-500 focus:ring-offset-2 rounded-lg p-1"n            >n              <img n                src="/images/greybeez-logo.JPG" n                alt="GreyBeez Pvt. Ltd. - Empowering Mankind"n                className="h-16 md:h-20 w-auto object-contain opacity-70"n                onError={(e) => {n                  console.error('Failed to load GreyBeez logo, falling back to text');n                  // Fallback to text if image fails to loadn                  e.currentTarget.style.display = 'none';n                  e.currentTarget.nextElementSibling!.style.display = 'block';n                }}n              />n              <span n                className="text-2xl md:text-3xl font-bold text-primary-600 hidden"n                style={{ display: 'none' }}n              >n                GreyBeezn              </span>n            </button>n          </div>nn          {/* Desktop Navigation */}n          <nav className="hidden md:flex items-center space-x-8">n            {navigation.map((item) => (n              <buttonn                key={item.key}n                onClick={() => onPageChange(item.key)}n                className={`text-lg font-semibold transition-colors duration-300 whitespace-nowrap ${n                  currentPage === item.keyn                    ? 'text-purple-800 border-b-2 border-purple-800 pb-1'n                    : 'text-purple-800 hover:text-accent-orange-500 hover:border-b-2 hover:border-accent-orange-500'n                }`}n              >n                {item.name}n              </button>n            ))}n          </nav>nn          {/* Language Switcher & Mobile Menu Button */}n          <div className="flex items-center space-x-4">n            <LanguageSwitcher />n            n            {/* JobPar.ai Button */}n            <buttonn              onClick={() => onPageChange('jobpar')}n              className={`px-6 py-3 rounded-xl transition-all duration-300 flex items-center space-x-2 ${n                currentPage === 'jobpar'n                  ? 'text-white bg-gradient-to-br from-gray-600 via-accent-orange-500 to-green-500 border-2 border-gray-400 shadow-lg'n                  : 'text-white bg-gradient-to-br from-gray-500 via-accent-orange-500 to-green-400 hover:from-gray-600 hover:via-accent-orange-600 hover:to-green-500 hover:scale-105 shadow-md hover:shadow-lg'n              }`}n              title="JobPar.ai - Mobile App"n            >n              <Smartphone className="w-5 h-5" />n              <span className="font-semibold text-sm">JobPar.ai</span>n            </button>n            n            {/* Mobile menu button */}n            <buttonn              onClick={() => setIsMenuOpen(!isMenuOpen)}n              className="md:hidden p-2 rounded-lg text-purple-800 hover:text-accent-orange-500 hover:bg-accent-orange-500/10 transition-colors duration-300"n            >n              {isMenuOpen ? <X className="w-6 h-6" /> : <Menu className="w-6 h-6" />}n            </button>n          </div>n        </div>nn        {/* Mobile Navigation */}n        {isMenuOpen && (n          <div className="md:hidden py-4 border-t border-purple-200 bg-white/95 backdrop-blur-md">n            <nav className="flex flex-col space-y-2">n              {navigation.map((item) => (n                <buttonn                  key={item.key}n                  onClick={() => {n                    onPageChange(item.key);n                    setIsMenuOpen(false);n                  }}n                  className={`text-left px-4 py-2 text-lg font-semibold rounded-lg transition-colors duration-300 ${n                    currentPage === item.keyn                      ? 'text-purple-800 bg-purple-100'n                      : 'text-purple-800 hover:text-accent-orange-500 hover:bg-accent-orange-500/10'n                  }`}n                >n                  {item.name}n                </button>n              ))}n              n              {/* JobPar.ai Mobile Button */}n              <buttonn                onClick={() => {n                  onPageChange('jobpar');n                  setIsMenuOpen(false);n                }}n                className={`text-left px-4 py-2 text-lg font-semibold rounded-lg transition-all duration-300 flex items-center ${n                  currentPage === 'jobpar'n                    ? 'text-white bg-gradient-to-br from-gray-600 via-accent-orange-500 to-green-500'n                    : 'text-purple-800 hover:text-accent-orange-500 hover:bg-accent-orange-500/10'n                }`}n              >n                <Smartphone className="w-5 h-5 mr-2" />n                JobPar.ain              </button>n            </nav>n          </div>n        )}n      </div>n    </header>n  );n}